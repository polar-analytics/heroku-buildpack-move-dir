# heroku-buildpack-move-dir

Move a given `BUILDPACK_MOVE_DIR_FROM` directory to `BUILDPACK_MOVE_DIR_TO`.

## Usage

Set environment variables `BUILDPACK_MOVE_DIR_FROM` and `BUILDPACK_MOVE_DIR_TO`.

## License

MIT
